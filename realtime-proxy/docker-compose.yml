version: '3.8'
services:
  realtime-proxy:
    build: .
    image: realtime-proxy:latest
    container_name: realtime-proxy
    ports:
      - "8080:8080"
    environment:
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_KEY=${SUPABASE_KEY}
    restart: unless-stopped
